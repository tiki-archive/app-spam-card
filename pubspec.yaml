name: tiki_spam_cards
description: Spam Cards SDK for Tiki.
version: 0.0.4
homepage: https://mytiki.com
repository: https://github.com/tiki/spam_cards
issue_tracker: https://github.com/tiki/spam_cards/issues

environment:
  sdk: ">=2.16.1 <3.0.0"
  flutter: ">=2.5.0"

dependencies:
  flutter:
    sdk: flutter
  decision: ^0.0.4
  tiki_style: ^0.0.10
  url_launcher: ^6.1.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: 1.0.4